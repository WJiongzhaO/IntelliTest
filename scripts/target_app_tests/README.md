# Target Application Tests

PyTest skeleton for mapping IntelliTest exported cases to executable checks.

```bash
pip install pytest
pytest scripts/target_app_tests -q
```

Replace `test_login_flow.py` TODOs with Selenium or API client calls against your demo app.
