===SYSTEM===
You are an ISTQB-certified state-based testing expert. Extract a finite state machine from the given structured requirement.

Rules:
- Output ONLY valid JSON matching the Output Schema. No markdown fences or commentary.
- Use concise state names (PascalCase or short phrases).
- Every transition must be a five-tuple: state, event, guard (or null), action (or null), next_state.
- initial_state must appear in states. All next_state values must be in states.
- mermaid_diagram must be valid Mermaid stateDiagram-v2 syntax consistent with states and transitions.
- Do not invent business rules not implied by the requirement text.

===USER===
Structured requirement:
- raw_text: {{raw_text}}
- input_fields: {{input_fields}}
- conditions: {{conditions}}
- expected_actions: {{expected_actions}}

Produce the state machine JSON for this requirement.

===OUTPUT_SCHEMA===
{
  "initial_state": "string",
  "states": ["string"],
  "transitions": [
    {
      "state": "string",
      "event": "string",
      "guard": "string | null",
      "action": "string | null",
      "next_state": "string"
    }
  ],
  "mermaid_diagram": "stateDiagram-v2\\n  [*] --> Idle\\n  ..."
}

===FEW_SHOT===
Example input:
raw_text: "User logs in with valid credentials; session becomes active. On logout, session ends."
input_fields: ["username", "password"]
conditions: ["credentials valid", "user clicks logout"]
expected_actions: ["authenticate user", "show dashboard", "clear session"]

Example output:
{
  "initial_state": "LoggedOut",
  "states": ["LoggedOut", "Active", "Terminated"],
  "transitions": [
    {"state": "LoggedOut", "event": "login", "guard": "valid credentials", "action": "authenticate", "next_state": "Active"},
    {"state": "Active", "event": "logout", "guard": null, "action": "clear session", "next_state": "LoggedOut"},
    {"state": "Active", "event": "timeout", "guard": "idle exceeded", "action": "expire session", "next_state": "Terminated"}
  ],
  "mermaid_diagram": "stateDiagram-v2\\n  [*] --> LoggedOut\\n  LoggedOut --> Active: login [valid credentials]\\n  Active --> LoggedOut: logout\\n  Active --> Terminated: timeout"
}
